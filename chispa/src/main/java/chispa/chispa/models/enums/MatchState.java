package chispa.chispa.models.enums;

/**
 * Enum representing the state of a match between users.
 * MATCHED: Users have matched.
 * BLOCKED: The match is blocked.
 */
public enum MatchState {
    /**
     * Users have matched
     */
    MATCHED,
    /** The match is blocked */
    BLOCKED,
}
